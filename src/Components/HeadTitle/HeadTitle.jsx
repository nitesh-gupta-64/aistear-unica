import React from 'react'

const HeadTitle = ({headTitle}) => {
  return (
    <div>
        <h2 style={{margin: '2rem 5rem 0.5rem 5rem', fontSize: '2rem', fontWeight:'400'}}>{headTitle}</h2>
    </div>
  )
}

export default HeadTitle